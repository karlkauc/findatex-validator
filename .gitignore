# Maven
target/
*.class
*.jar.original

# Frontend build artefacts (web-app/src/main/frontend/)
node_modules/
.tsc-out/
*.tsbuildinfo
dist/

# Playwright MCP scratch + ad-hoc screenshots
.playwright-mcp/
validator-e2e-*.png

# IDE
.idea/
*.iml
.vscode/
.classpath
.project
.settings/
.factorypath
nbproject/

# OS
.DS_Store
Thumbs.db
._*

# Logs
*.log

# Claude Code scratch / per-project data
.claude/

# WebDAV / production data drops — never commit real fund files.
.DAV/
*_TPTV7_*.xlsx
tools/__pycache__/

# Local dev tooling not part of the build (Playwright scraper, dirty-tests
# harness, ad-hoc screenshots). The scraper has its own node_modules and
# runs/ output dir; the dirtyTests/ fixtures contain real UBS fund files.
GEMINI.md
/package.json
/package-lock.json
scraper/
dirtyTests/
dirtyTests_results.json
validate_dirty_tests.py
core/src/test/java/com/findatex/validator/validation/DirtyTestsValidationTest.java
/Screenshot *.png
/quota-exhausted.png
