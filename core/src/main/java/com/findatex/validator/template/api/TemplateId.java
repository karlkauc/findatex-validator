package com.findatex.validator.template.api;

/** Identifies a FinDatEx template family. Versions live separately on {@link TemplateVersion}. */
public enum TemplateId {
    TPT,
    EET,
    EMT,
    EPT
}
