<?xml version="1.0" encoding="UTF-8"?>
<!--
  Canonical URL list. Every <loc> must use the canonical host
  (findatex.web.canonical-host) — a sitemap entry on the non-canonical host
  contradicts the 301 in CanonicalHostFilter and is dropped by crawlers.
  Extend this when the generated rule reference gets real URLs.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://www.findatex-validator.eu/</loc>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
